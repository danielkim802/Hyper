var len = fun arr {
	$ load_name arr
	$ len_arr
	$ return
}

var map = fun f arr {
	var res = []
	for i=0 to len arr { res += [f arr[i]] }
	return res
}

var input = fun prompt {
	$ load_name prompt
	$ input
	$ return
}

var exit = fun {
	$ exit
}